server:
  port: 8084

spring:
  datasource:
    url: jdbc:h2:file:~\testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console.enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

    hibernate:
        ddl-auto: update